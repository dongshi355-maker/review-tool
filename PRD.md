# 视频素材评审工具 PRD

> 版本 v1.0 | 2026-05-25 | Red

---

## 1. 项目背景

NXB 定期向钉钉群发送 15-30 条视频素材供同事评审。现有流程中，视频素材与评分问卷分离，同事需要来回切换，体验差、效率低。同时希望将评分与素材下载绑定，提升参与度。

## 2. 项目目标

创建一个在线评审工具，实现素材浏览、评分、建议收集一体化。同事打开一个链接即可完成全部评审，提交后获得素材百度云下载链接。

## 3. 用户角色

| 角色 | 描述 |
|------|------|
| **管理员 (NXB)** | 创建评审批次、配置评分维度、查看评审结果、导出数据 |
| **评审人** | 打开评审链接、逐条评分、提交后获取下载链接 |

## 4. 功能清单

### 4.1 管理端功能

#### F-01 新建评审批次
- 输入：批次名称（如"第3批素材"）、素材列表（名称 + 可选描述）
- 设置评分维度（默认：画质/构图/色彩/可用性，可增删改）
- 设置访问码（可选，用于限制访问）
- 设置百度云链接（提交后对评审人可见）
- 生成唯一评审链接

#### F-02 管理评审批次
- 查看所有已创建的批次
- 修改批次信息（素材列表、评分维度、百度云链接等）
- 关闭/开启批次（关闭后不再接受新提交）
- 删除批次

#### F-03 查看评审结果
- 按批次查看所有评审人的提交
- 数据展示：
  - 每条素材的平均分、各维度得分
  - 是否选用统计（选用数/总数）
  - 建议汇总（按素材分组）
- 排名：按综合得分排出素材 Top N

#### F-04 导出数据
- 导出 CSV/Excel，包含所有评分明细
- 格式：批次 → 素材 → 评审人 → 各维度分 → 建议 → 是否选用

### 4.2 评审端功能

#### F-05 进入评审
- 打开链接 → 输入访问码（如批次配置了访问码）→ 进入评审页
- 页面顶部显示批次名称

#### F-06 逐条评审素材
- 每条素材卡片包含：
  - 素材序号 + 名称
  - 各维度评分（1-5 星 / 滑动条）
  - 是否选用（是/否）
  - 文字建议（可选）
- 评分即时保存（离开页面不丢失）
- 支持未全部评完时暂存

#### F-07 提交评审
- 所有素材评完后方可提交
- 提交前校验：是否有未评素材？提示
- 提交成功后显示百度云下载链接
- 提交后不可修改

#### F-08 查看下载链接
- 提交成功后页面展示百度云链接
- 评审人可复制链接自行下载

## 5. 非功能性需求

### 5.1 性能
- 首屏加载 < 3 秒（国内网络）
- 评分操作即时响应，无感知延迟

### 5.2 安全
- 访问码保护（可选开启）
- 提交后不可修改（防止刷分）
- 不做严格的身份鉴权，但未来可扩展钉钉 OAuth

### 5.3 兼容性
- 移动端优先（钉钉内打开为主）
- 支持 iOS Safari、Android Chrome、桌面浏览器

### 5.4 数据
- 所有评分数据持久化存储
- 支持导出
- 免费额度内运行

## 6. 技术方案

| 层 | 选型 |
|------|------|
| 前端框架 | React + TypeScript + Vite |
| 样式 | TailwindCSS |
| UI 组件 | 自建 / shadcn/ui |
| 路由 | React Router |
| 后端/数据库 | Supabase (PostgreSQL) |
| 部署 | Vercel |
| 状态管理 | React Context + useReducer |

## 7. 数据库设计

### 7.1 表结构

```sql
-- 评审批次
batches (
  id            uuid PRIMARY KEY,
  name          text NOT NULL,           -- 批次名称
  access_code   text,                    -- 访问码 (null=无限制)
  baidu_link    text,                    -- 百度云链接
  rating_dimensions jsonb NOT NULL,      -- [{key, label, type: 'star'|'yesno'}]
  materials     jsonb NOT NULL,          -- [{id, name, description?}]
  is_active     boolean DEFAULT true,    -- 是否开放
  created_at    timestamptz,
  updated_at    timestamptz
)

-- 评审提交
submissions (
  id            uuid PRIMARY KEY,
  batch_id      uuid REFERENCES batches(id),
  reviewer_name text,                    -- 评审人昵称
  ratings       jsonb NOT NULL,          -- [{material_id, dimension_key: value, suggestion?, selected?}]
  submitted_at  timestamptz
)
```

## 8. 页面路由

| 路由 | 页面 | 说明 |
|------|------|------|
| `/` | 管理首页 | 查看所有批次，新建批次入口 |
| `/batch/new` | 新建批次 | 配置批次信息 |
| `/batch/:id` | 批次详情 | 查看某批次评审结果 |
| `/review/:id` | 评审页 | 评审人打分页 |

## 9. 默认评分维度

```json
[
  { "key": "quality",    "label": "画质",   "type": "star", "max": 5 },
  { "key": "composition","label": "构图",   "type": "star", "max": 5 },
  { "key": "color",      "label": "色彩",   "type": "star", "max": 5 },
  { "key": "usability",  "label": "可用性", "type": "star", "max": 5 },
  { "key": "selected",   "label": "是否选用","type": "yesno" }
]
```

## 10. 视频托管

素材视频需上传至腾讯云 COS 获取直链 URL，用于网页内嵌入播放。

- 存储桶设置为公有读
- 视频格式建议 MP4 (H.264)，兼容所有浏览器和移动端
- 流量费用预估：15-30条 × 200MB × 10人预览 ≈ 30-60GB，约 15-30 元/批
- 评审页仅提供在线预览播放，不提供视频文件下载（下载走百度云）

## 11. MVP 范围

| 包含 | 暂不包含 |
|------|------|
| 新建/管理批次 | 钉钉 OAuth 登录 |
| 自定义评分维度 | 评审人身份管理 |
| 评审页打分+建议 | 批量导入素材 |
| 视频嵌入播放 (COS 直链) | 数据可视化图表 |
| 访问码保护 | 邮件/钉钉通知 |
| 百度云链接解锁 | |
| 结果查看+导出 | |

## 12. 开发计划

| 阶段 | 内容 | 预估 |
|------|------|------|
| Phase 1 | 项目初始化 + 数据库 + 管理端（新建/查看批次） | Day 1 |
| Phase 2 | 评审端（打分页 + 提交流程） | Day 1-2 |
| Phase 3 | 结果查看 + 数据导出 | Day 2 |
| Phase 4 | 样式打磨 + 移动端适配 + 部署 | Day 2-3 |

---

_下一步：确认 PRD 后进入 Phase 1 开发。_
